version: "3.8"

services:
  app:
    build: .
    command: ./run.sh
    volumes:
      - ./app/:/usr/src/app/
    env_file:
      - .env
    ports:
      - "8001:8001"
    depends_on:
      - database


  database:
    image: postgres:13.0-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - .env
    ports:
      - "5433:5432"

volumes:
  postgres_data: